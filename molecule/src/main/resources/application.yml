server:
  port: 8002
spring:
  application:
    name: molecule
  datasource:
    url: jdbc:mysql://172.31.18.111:3306/molecule
    username: root
    password: root
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

eureka:
  instance:
    preferIpAddress: true
  client:
    serviceUrl:
      defaultZone: http://172.31.18.111:8070/eureka/

info:
  app:
    name: "molecule"
    description: "InChemi Molecule Application"
    version: "1.0.0"